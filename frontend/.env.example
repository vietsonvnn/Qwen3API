# Backend API URL
# Local dev: để trống (Vite proxy /api → localhost:3001)
# Production: đặt là Railway backend URL
VITE_API_URL=

# Google OAuth Client ID (Web application — phải khớp với GOOGLE_CLIENT_ID ở backend)
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
